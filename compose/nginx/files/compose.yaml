services:
  << (service_name | default('nginx', true)) >>:
    image: docker.io/library/nginx:1.30.0-alpine
    restart: << restart_policy >>
    container_name: << container_name >>
    <%- if traefik_enabled %>
    networks:
      << traefik_network >>:
    <%- endif %>
    <%- if not traefik_enabled %>
    ports:
      - "<< ports_http >>:80"
      - "<< ports_https >>:443"
    <%- endif %>
    # volumes:
    #   - ./config/default.conf:/etc/nginx/conf.d/default.conf:ro
    #   - ./data:/usr/share/nginx/html:ro
    <%- if traefik_enabled %>
    labels:
      - traefik.enable=true
      - traefik.docker.network=<< traefik_network >>
      - traefik.http.services.<< (service_name | default('nginx', true)) >>-web.loadbalancer.server.port=80
      - traefik.http.routers.<< (service_name | default('nginx', true)) >>-web-http.rule=Host(`<< traefik_host >>.<< traefik_domain >>`)
      - traefik.http.routers.<< (service_name | default('nginx', true)) >>-web-http.entrypoints=<< traefik_entrypoint >>
      - traefik.http.routers.<< (service_name | default('nginx', true)) >>-web-http.service=<< (service_name | default('nginx', true)) >>-web
      <%- if traefik_tls_enabled %>
      - traefik.http.routers.<< (service_name | default('nginx', true)) >>-web-https.rule=Host(`<< traefik_host >>.<< traefik_domain >>`)
      - traefik.http.routers.<< (service_name | default('nginx', true)) >>-web-https.entrypoints=<< traefik_tls_entrypoint >>
      - traefik.http.routers.<< (service_name | default('nginx', true)) >>-web-https.tls=true
      - traefik.http.routers.<< (service_name | default('nginx', true)) >>-web-https.tls.certresolver=<< traefik_tls_certresolver >>
      - traefik.http.routers.<< (service_name | default('nginx', true)) >>-web-https.service=<< (service_name | default('nginx', true)) >>-web
      <%- endif %>
    <%- endif %>

<%- if traefik_enabled %>
networks:
  << traefik_network >>:
    external: true
<%- endif %>
