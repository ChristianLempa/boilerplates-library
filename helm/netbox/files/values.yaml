---
image:
  repository: docker.io/netboxcommunity/netbox
  tag: v4.5.10
  pullPolicy: IfNotPresent

replicaCount: 1

superuser:
  name: << netbox_superuser_name >>
  email: << netbox_superuser_email >>
  existingSecret: << netbox_existing_secret >>

allowedHosts:
  - "<< netbox_allowed_hosts >>"

<%- if database_external %>
postgresql:
  enabled: false

externalDatabase:
  host: << database_host >>
  port: << database_port >>
  database: << database_name >>
  username: << database_user >>
  existingSecretName: << database_secret_name >>
  existingSecretKey: password
<%- else %>
postgresql:
  enabled: true
  auth:
    database: << database_name >>
    username: << database_user >>
    existingSecret: << database_secret_name >>
<%- endif %>

<%- if redis_external %>
redis:
  enabled: false


tasksRedis:
  host: << redis_host >>
  port: 6379
  database: 0
  existingSecretName: << redis_secret_name >>
  existingSecretKey: redis-password

cachingRedis:
  host: << redis_cache_host >>
  port: 6379
  database: 1
  existingSecretName: << redis_secret_name >>
  existingSecretKey: redis-password
<%- else %>
redis:
  enabled: true
  architecture: standalone
  auth:
    enabled: true
    existingSecret: << redis_secret_name >>
    existingSecretPasswordKey: redis-password
<%- endif %>

existingSecret: << netbox_existing_secret >>

<%- if email_enabled %>
email:
  server: << email_host >>
  port: << email_port >>
  username: << email_username >>
  existingSecretName: << netbox_existing_secret >>
  existingSecretKey: email-password
  from: << email_from >>
  useSSL: << email_use_ssl | lower >>
  useTLS: << email_use_tls | lower >>
<%- endif %>

<%- if netbox_cors_enabled %>
cors:
  originAllowAll: true
  originWhitelist:
    - << netbox_cors_origins >>
<%- endif %>

<%- if netbox_metrics_enabled %>
metrics:
  enabled: true
  serviceMonitor:
    enabled: false
<%- endif %>

service:
  type: << network_mode >>

<%- if traefik_enabled %>
ingress:
  enabled: true
  className: traefik
<%- if traefik_tls_enabled and traefik_tls_certmanager %>
  annotations:
    cert-manager.io/cluster-issuer: << certmanager_issuer >>
<%- endif %>
  hosts:
    - host: << traefik_host >>
      paths:
        - path: /
          pathType: Prefix
<%- if traefik_tls_enabled %>
  tls:
    - secretName: << traefik_tls_secret >>
      hosts:
        - << traefik_host >>
<%- endif %>
<%- endif %>

persistence:
  enabled: true
  <%- if volumes_mode == 'existing-pvc' %>
  existingClaim: << volumes_pvc_name >>
  <%- else %>
  storageClass: ""
  size: 10Gi
  <%- endif %>

worker:
  enabled: true
  replicaCount: 1

housekeeping:
  enabled: true
